{
  "name": "orbital-end2end",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "COMPONENT_PREVIEW_E2E=1 npx playwright test",
    "test:preview": "npm run test -- --project=component-preview",
    "test:preview:all": "npm run test -- --project=component-preview"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.1",
    "@playwright/test": "^1.44.1",
    "@types/node": "^24.0.0",
    "typescript": "^5.4.5"
  }
}
