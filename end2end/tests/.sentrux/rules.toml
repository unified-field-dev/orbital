[constraints]
max_cycles = 0
max_cc = 25
no_god_files = true

[[boundaries]]
from = "components/**"
to = "components/**"
reason = "Spec files must not import other spec files"

[[boundaries]]
from = "smoke/**"
to = "components/**"
reason = "Smoke tests must not import component spec files"
